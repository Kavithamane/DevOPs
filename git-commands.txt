	# git --version 

	# git -v 

	# git init 

	# git config --global user.name "kavitha" 

	# git config --global user.email "kavitha@gmai.com" 
	
	# git config --list

	# git status 
	
        # git add <file-name> 

	# git add .

	# git rm --cached <file-name>  

	# git restore <file-name>
	
	# git restore --stage <file-name> 
	
	# git commit -m "Message" 
	
	# git show 
	
	# git log 

	# git log --online 

	# git show <commit-ID> 

	# git remote -v 

	# git remote add origin https://github.com/Kavithamane/DevOPs-.git

	# git branch 

	# git branch -M main 

	# git config pull.rebase true		=> # pulling file from remote repo 
	
	# git push -u origin <remote-branch-name>  

        # git push <remote-url>

	# git push origin main:Branch1		=> Push file from main local branch to remote another branch 
 
	# git push origin new_branch:Branch1	=> Push file from local another branch to remote another branch 

	# git push <url> 

	# git push -f <url> 

	# git pull <url> 

	# git pull origin main 

	# git log --grep "london"

	# git log -1 

	# git log -2 

	# git branch 
	
	# git branch <new-branch

	# git push origin new-branch-local:main-remote
	
	# # git push origin master-local:main-remote 

	# git push <url> 

	# git push -f <url> 

	# git pull <url> 

	# git pull origin main 

	# git log --grep "london"

	# git log -1 

	# git log -2 

	# git branch 
	
	# git branch <new-branch> 

	# git checkout <branch-name> 

	# git branch -d <branch-name> 

	# git branch -b <branch-name> 
	
	# git checkout -b new-branch> 

	# git revert <commit-id> 

	# git merge <branch-name> 

	# git stash 

	# got stash list 

	# git stash apply stash@{1}
	
	# git stash apply stash@{0} 
	
	# git stash clear 

	# git reset <file-name> --one file

	# git reset . 
	
	# git reset --hard 

	# git rm <file-name> 

	# git clean -n 

	# gir clean -f 

	# git tag 
	
	# git tag -a tag-name -m "tagmessage" commit-id

	# git tag -d test 

	# git clone <url> 


 
